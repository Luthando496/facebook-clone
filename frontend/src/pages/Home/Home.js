import React, { useState } from "react";
import Header from "../../Components/Header";

// import { useClickOutSide } from "../../helpers/clickOutSide";

const Home = () => {
  // const [showSeach, setShow] = useState(false);
  return (
    <div>
      <Header />
    </div>
  );
};

export default Home;
